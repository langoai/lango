## 1. Docs Sync

- [x] 1.1 Update `README.md` so tool lifecycle visibility mentions approval-wait and canceled states with persistent param previews.
- [x] 1.2 Update `docs/cli/core.md` so the same broader lifecycle wording appears in the CLI overview.

## 2. Spec Sync

- [x] 2.1 Record the first-touch tool-lifecycle-state documentation requirement in the OpenSpec delta.

## 3. Verification

- [x] 3.1 Run `openspec validate readme-cli-chat-tool-lifecycle-state-sync --strict`.
- [x] 3.2 Validate and archive the OpenSpec change.
