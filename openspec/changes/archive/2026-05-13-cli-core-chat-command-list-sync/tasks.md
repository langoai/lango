## 1. Docs Sync

- [x] 1.1 Update the CLI core overview to include `/mode` and `/cost` in the chat slash-command summary.
- [x] 1.2 Update the downstream-docs-sync spec to require the same CLI-overview command coverage.

## 2. Verification

- [x] 2.1 Run `openspec validate cli-core-chat-command-list-sync --strict`.
- [x] 2.2 Archive the OpenSpec change.
- [x] 2.3 Run `openspec validate --specs`.
