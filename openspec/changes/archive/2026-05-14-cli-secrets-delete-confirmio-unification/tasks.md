## 1. Shared Confirmation Integration

- [x] 1.1 Replace the inline `secrets delete` confirmation parser with the shared prompt helper
- [x] 1.2 Preserve the existing `--force` safeguard and prompt text

## 2. Verification

- [x] 2.1 Add regressions for abort, confirm, force, and non-interactive guidance
- [x] 2.2 Update the public secrets CLI documentation note
- [x] 2.3 Validate with package tests, repository-wide build/test, and OpenSpec checks
