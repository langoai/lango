## 1. Docs Sync

- [x] 1.1 Update cockpit feature docs to describe the Chat page operator surface.
- [x] 1.2 Update the downstream-docs-sync spec to require the same Chat coverage.

## 2. Verification

- [x] 2.1 Run `openspec validate cockpit-chat-surface-docs-sync --strict`.
- [x] 2.2 Archive the OpenSpec change.
- [x] 2.3 Run `openspec validate --specs`.
