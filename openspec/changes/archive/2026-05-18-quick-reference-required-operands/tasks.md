## 1. Tests

- [x] 1.1 Add failing docs guard coverage for `lango memory clear <session-key>` in README, CLI index, and affected feature docs.
- [x] 1.2 Add failing docs guard coverage for P2P firewall/session quick-reference required peer operands in README, CLI index, and affected feature docs.
- [x] 1.3 Add failing docs guard coverage for full `config get` output/show-secrets usage in README and CLI index.

## 2. Documentation

- [x] 2.1 Update README quick references and affected prose to include the required operands and flags.
- [x] 2.2 Update `docs/cli/index.md` and affected feature docs to include the required operands and flags.

## 3. OpenSpec And Verification

- [x] 3.1 Add delta specs for public quick-reference required operands and executable guard coverage.
- [x] 3.2 Run focused tests, full Go build/tests, and strict OpenSpec validation.
- [x] 3.3 Run subagent-driven review and address required findings.
- [x] 3.4 Sync specs, archive the change, and commit the scoped unit.
