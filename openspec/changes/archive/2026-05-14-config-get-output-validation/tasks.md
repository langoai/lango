## 1. Implementation

- [x] 1.1 Validate `lango config get --output` as `plain|json`
- [x] 1.2 Add regression coverage proving invalid output values fail before config loading

## 2. Sync

- [x] 2.1 Update config docs and OpenSpec references from stale `--format json` wording to `--output json`
