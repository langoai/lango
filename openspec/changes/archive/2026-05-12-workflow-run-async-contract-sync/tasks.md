## 1. Contract Sync

- [x] 1.1 Add `workflow_run` wrapper regressions for OR-input validation and async receipt shape.
- [x] 1.2 Update workflow tool spec wording to match the async launch contract.

## 2. Verification

- [x] 2.1 Run `go test ./internal/workflow -count=1`.
- [x] 2.2 Run `go build ./...`.
- [x] 2.3 Run `go test ./...`.

## 3. Spec Sync

- [x] 3.1 Update `automation-agent-tools` coverage for `workflow_run`'s async contract.
- [ ] 3.2 Validate and archive the OpenSpec change.
