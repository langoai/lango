## Overview

This is a small interaction refinement for the in-flight follow-up loop.

## Design Decisions

### Running follow-up selection should mirror armed-starter selection

Before submission, starter prompts can already be replaced freely with `1/2/3`. The same replacement affordance now remains available after submission while a follow-up draft is staged, so the operator does not have to manually clear the draft first.
