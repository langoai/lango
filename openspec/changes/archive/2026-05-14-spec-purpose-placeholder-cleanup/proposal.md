## Why

Two main specs still carry archive-placeholder `Purpose` text instead of real summaries. That weakens discoverability and makes the spec set look partially unfinished even though the requirements themselves are current.

## What Changes

- Replace the placeholder purpose text in `cli-prompt-helpers`
- Replace the placeholder purpose text in `shared-line-reader`
- Record the cleanup as docs/spec hygiene in OpenSpec

## Capabilities

### New Capabilities

### Modified Capabilities
- `docs-only`: main specs no longer retain archive placeholder purpose text

## Impact

- Affected specs: `openspec/specs/cli-prompt-helpers/spec.md`, `openspec/specs/shared-line-reader/spec.md`
- No code or runtime behavior changes
