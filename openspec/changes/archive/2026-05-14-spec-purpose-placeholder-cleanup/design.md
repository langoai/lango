## Overview

This is a spec-hygiene cleanup. The requirements are already correct; only the high-level `Purpose` summaries still contain generated placeholder text from archived changes.

## Decision

- Replace placeholder text with concise summaries that describe the scope of each spec
- Keep all requirement bodies unchanged

## Consequences

- Faster navigation for maintainers reading the spec index
- Cleaner main specs after archive-driven evolution
