## 1. Docs Sync

- [x] 1.1 Update `README.md` so approval transcript events mention compact request-id annotations.
- [x] 1.2 Update `docs/cli/core.md` so the CLI overview mentions the same approval traceability behavior.

## 2. Spec Sync

- [x] 2.1 Record the first-touch approval-traceability documentation requirement in the OpenSpec delta.

## 3. Verification

- [x] 3.1 Run `openspec validate readme-cli-approval-traceability-sync --strict`.
- [x] 3.2 Validate and archive the OpenSpec change.
