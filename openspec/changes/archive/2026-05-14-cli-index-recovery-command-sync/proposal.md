## Why

The recovery setup and restore commands are implemented and documented in the security CLI page, but they are still missing from the public CLI quick reference.

## What Changes

- add `lango security recovery setup` and `lango security recovery restore` to `docs/cli/index.md`

## Impact

- improves discoverability for a critical recovery workflow
- reduces mismatch between detailed command docs and the quick reference
