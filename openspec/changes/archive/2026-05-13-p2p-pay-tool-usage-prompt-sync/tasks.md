## 1. Prompt Sync

- [x] 1.1 Update `TOOL_USAGE.md` so `p2p_pay` documents required `transaction_receipt_id`.

## 2. Verification

- [x] 2.1 Run `openspec archive p2p-pay-tool-usage-prompt-sync -y`.
- [x] 2.2 Run `openspec validate --specs`.

## 3. Spec Sync

- [x] 3.1 Update `p2p-agent-prompts` coverage for the `p2p_pay` required-input wording.
