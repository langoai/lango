## MODIFIED Requirements

### Requirement: RetrievalConfig surface (MODIFIED — Shadow field removed)
The `RetrievalConfig` SHALL contain `Enabled`, `Feedback`, and `AutoAdjust` fields only. The `Shadow` field was removed in Step 15 (legacy-path-cleanup). The coordinator no longer supports shadow mode.
