## 1. Main Spec Sync

- [x] 1.1 Update the cockpit-pages spec to describe explicit Dead Letters load-failure messaging.

## 2. Verification

- [x] 2.1 Run `openspec validate deadletters-main-spec-load-failure-sync --strict`.
- [x] 2.2 Archive the OpenSpec change.
- [x] 2.3 Run `openspec validate --specs`.
