## 1. Shared Confirmation Integration

- [x] 1.1 Replace the inline `graph clear` confirmation parser with the shared prompt helper
- [x] 1.2 Preserve the warning line and `--force` bypass

## 2. Verification

- [x] 2.1 Validate graph CLI regressions still cover abort/confirm/force command-stream behavior
- [x] 2.2 Validate with package tests, repository-wide build/test, and OpenSpec checks
