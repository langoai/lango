## 1. Tests

- [x] 1.1 Add failing chat slash-command coverage for MCP active runtime status.
- [x] 1.2 Add coverage that configured MCP without runtime remains non-active.

## 2. Implementation

- [x] 2.1 Add a runtime feature snapshot to chat dependencies and status rendering.
- [x] 2.2 Pass MCP runtime state from chat, cockpit, and workbench entrypoints.

## 3. Documentation And Specs

- [x] 3.1 Update public TUI docs and OpenSpec deltas for MCP runtime status.
- [x] 3.2 Run focused tests, full Go build/tests, and strict OpenSpec validation.
- [x] 3.3 Run subagent-driven review and address required findings.
- [x] 3.4 Sync specs, archive the change, and commit the scoped unit.
