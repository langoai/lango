## 1. Wrapper Param Guards

- [x] 1.1 Add `view_skill` wrapper coverage for missing `name`.
- [x] 1.2 Add `import_skill` wrapper coverage for missing `url`.

## 2. Verification

- [x] 2.1 Run `go test ./internal/app -count=1`.
- [x] 2.2 Run `go build ./...`.
- [x] 2.3 Run `go test ./...`.

## 3. Spec Sync

- [x] 3.1 Update `meta-tools` and `production-readiness` coverage for skill read/import wrapper params.
- [ ] 3.2 Validate and archive the OpenSpec change.
