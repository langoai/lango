## 1. Docs Sync

- [x] 1.1 Update `docs/cli/index.md` so the bare-`lango` reference reflects setup recovery and starter-prompt quick start.
- [x] 1.2 Update `docs/getting-started/quickstart.md` so the interactive TUI tip reflects the same readiness split.

## 2. Spec Sync

- [x] 2.1 Record the first-touch workbench readiness documentation requirement in the OpenSpec delta.

## 3. Verification

- [x] 3.1 Run `openspec validate cli-workbench-readiness-docs-sync --strict`.
- [x] 3.2 Validate and archive the OpenSpec change.
