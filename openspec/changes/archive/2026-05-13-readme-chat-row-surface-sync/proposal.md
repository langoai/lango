## Why

The public cockpit feature docs now describe the Chat page's key surface and approval controls, but the README cockpit shortcut table still summarizes Chat only as a generic interactive conversation page. That leaves the most visible overview doc behind the current operator surface.

## What Changes

- Update the README cockpit shortcut table to mention the Chat page's slash-command and inline approval surface at a high level.
- Update the downstream-docs-sync spec to require this README Chat-row coverage.

## Capabilities

### New Capabilities

### Modified Capabilities

- `downstream-docs-sync`: README cockpit page descriptions need current Chat operator-surface wording.

## Impact

- Affected docs/specs: `README.md`, `openspec/specs/downstream-docs-sync/spec.md`
- No runtime code changes
