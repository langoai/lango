## 1. Implementation

- [x] 1.1 Buffer `zkexport --all` progress output until the run succeeds
- [x] 1.2 Add regression coverage proving failed `--all` runs leave stdout empty

## 2. Spec sync

- [x] 2.1 Update `zkp-core` to require failure-clean stdout for failed `--all` runs
