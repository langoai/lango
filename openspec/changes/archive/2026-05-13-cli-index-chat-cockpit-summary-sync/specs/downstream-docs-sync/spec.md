## MODIFIED Requirements

### Requirement: First-touch public docs mirror current CLI and cockpit operator surfaces
The repository SHALL keep first-touch public docs aligned with the runtime behavior and discoverability of the current CLI and cockpit entry surfaces.

#### Scenario: CLI index quick reference describes cockpit and chat surfaces concretely
- **WHEN** the CLI index quick-reference table describes `lango cockpit` and `lango chat`
- **THEN** those descriptions SHALL reflect the current multi-panel cockpit dashboard and focused chat transcript behavior rather than generic placeholders
