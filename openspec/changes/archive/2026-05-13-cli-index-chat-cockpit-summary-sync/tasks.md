## 1. Docs Sync

- [x] 1.1 Update `docs/cli/index.md` so `lango cockpit` mentions the richer multi-panel operator dashboard surface.
- [x] 1.2 Update `docs/cli/index.md` so `lango chat` mentions focused transcript, approval, and tool lifecycle visibility.

## 2. Spec Sync

- [x] 2.1 Record the CLI index quick-reference requirement in the OpenSpec delta.

## 3. Verification

- [x] 3.1 Run `openspec validate cli-index-chat-cockpit-summary-sync --strict`.
- [x] 3.2 Validate and archive the OpenSpec change.
