## Why

The CLI index quick-reference table still describes `lango cockpit` and `lango chat` in very shallow terms. More detailed CLI/docs now explain that cockpit includes Mission Control plus richer chat transcript surfaces, and that focused chat includes tool lifecycle rows and approval controls.

## What Changes

- Update the CLI index quick-reference descriptions for `lango cockpit` and `lango chat`.
- Record the sync in OpenSpec.

## Impact

- Improves first-touch command discovery in the CLI index.
- Keeps the quick-reference table closer to the actual operator experience.
