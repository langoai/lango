## Why

The chat transcript now carries both compact request-id annotations and compact request-summary previews on approval events, but the README and CLI core overview still only mention request-id traceability. That leaves first-touch docs one step behind the actual transcript contract.

## What Changes

- Update the README to mention compact request-summary previews on approval transcript events.
- Update the CLI core overview to mention the same richer approval traceability.
- Record the sync in OpenSpec.

## Impact

- Keeps first-touch docs aligned with the current approval transcript surface.
- Reduces drift between detailed feature docs and overview docs.
