## Overview

This is a docs-only truth-alignment change for the seeded-starter state.

## Design Decisions

### Document the real armed-state affordances

Once a starter prompt is armed, the operator can:

- press `Enter` to submit it
- press `1/2/3` to swap it for another starter prompt
- edit the text manually

The public docs now say that explicitly.
