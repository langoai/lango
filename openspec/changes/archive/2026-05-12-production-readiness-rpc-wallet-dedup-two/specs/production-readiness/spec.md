## REMOVED Requirements

### Requirement: RPC wallet address cleanup covers timeout and companion-error exits directly
**Reason**: Duplicate of the existing `RPC wallet address cleanup holds across timeout and companion-error exits` requirement.
**Migration**: No migration needed. The surviving requirement keeps the same behavioral contract.
