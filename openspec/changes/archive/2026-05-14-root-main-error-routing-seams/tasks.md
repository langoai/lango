## 1. Root Entrypoint Error Routing

- [x] 1.1 Add a seam-aware `runMain()` helper that returns exit codes
- [x] 1.2 Route broker-mode and root-command failures through injected stderr
- [x] 1.3 Add regressions for broker-mode and root-command failure paths
- [x] 1.4 Update spec coverage for the seam-aware root error path

## 2. Verification

- [x] 2.1 Validate with cmd/lango tests, repository-wide build/test, and OpenSpec checks
