# Tasks

- [x] Create the Proactive Agent Behavior Slice 3 OpenSpec change artifacts
- [x] Add the `mission-control-tui` delta for transient proposals, deterministic preparation, and durable acceptance preserving prepared context
- [x] Add the transient proposal domain with explicit proposal lifecycle and session-scoped registry
- [x] Add deterministic learning-suggestion preparation with prepared brief output
- [x] Wire the proposal registry into the app using learning suggestion as the only active producer
- [x] Make Mission Control read and render prepared proposals from the proposal registry
- [x] Accept prepared proposals into durable mission state while preserving the prepared brief context
- [x] Verify the change with `openspec validate proactive-agent-behavior-slice-three --strict`
