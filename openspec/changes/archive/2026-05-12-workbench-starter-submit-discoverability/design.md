## Overview

This is a copy-level UX clarification for the seeded-starter state.

## Design Decisions

### Distinguish seed phase from submit phase

The empty ready-profile workbench now exposes two distinct footer states:

- before seeding: `Enter default starter`
- after seeding: `Enter submits starter`

That matches actual runtime behavior and reduces ambiguity after the first keypress.
