## 1. Default Wrapper Hardening

- [x] 1.1 Update prompt package regressions for the default EOF-deny behavior
- [x] 1.2 Sync the main prompt helper spec with the default wrapper EOF-deny contract

## 2. Verification

- [x] 2.1 Validate with prompt package tests, repository-wide build/test, and OpenSpec checks
