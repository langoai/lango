## ADDED Requirements

### Requirement: README metrics inventory uses slash-form wording
The README internal CLI inventory SHALL describe the metrics family as `lango metrics/sessions/tools/agents/policy/history`.

#### Scenario: Stale bracket shorthand stays removed
- **WHEN** a maintainer updates the README internal tree inventory
- **THEN** it SHALL describe `lango metrics/sessions/tools/agents/policy/history` instead of bracket shorthand
