# Proposal

The pricing/negotiation/settlement audit slice is already reflected in the architecture docs. This change records that landed work in OpenSpec so the documentation ledger, landing page, and track status stay aligned.

## Scope

- Add an OpenSpec change for the pricing negotiation settlement audit slice.
- Sync the architecture landing-page requirement into `openspec/specs/project-docs/spec.md`.
- Sync the landed-track requirement into `openspec/specs/docs-only/spec.md`.
- Archive the completed change after the specs are updated.
