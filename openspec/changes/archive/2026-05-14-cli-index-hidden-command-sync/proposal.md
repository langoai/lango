## Why

Several implemented CLI surfaces are still absent from the public quick reference, which makes them effectively hidden to operators even though the commands ship today.

## What Changes

- add the implemented KMS wrap/detach commands to the CLI index
- add the implemented P2P workspace and provenance exchange commands to the CLI index

## Impact

- better discoverability for already-implemented operational commands
- less mismatch between the shipped CLI and the public reference
