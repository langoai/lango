## 1. Implementation

- [x] 1.1 Route `lango contract call` text output through `cmd.OutOrStdout()`
- [x] 1.2 Route `lango contract call --output` JSON output through `cmd.OutOrStdout()`
- [x] 1.3 Route the informational runtime note through `cmd.ErrOrStderr()`

## 2. Tests

- [x] 2.1 Add regression coverage for text stdout/stderr split capture
- [x] 2.2 Add regression coverage for JSON stdout/stderr split capture

## 3. Downstream

- [x] 3.1 Update public contract CLI docs for stream routing
- [x] 3.2 Update OpenSpec main spec with the stream-routing contract
- [x] 3.3 Add change proposal
- [x] 3.4 Add change design
- [x] 3.5 Add delta spec
