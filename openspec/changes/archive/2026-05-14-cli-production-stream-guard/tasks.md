## 1. CLI Stream Guard

- [x] 1.1 Add a repository test that rejects raw `fmt.Print*` calls in CLI production code
- [x] 1.2 Reject direct `os.Stdout`/`os.Stderr` references outside explicit seam files
- [x] 1.3 Update spec coverage for the executable guard

## 2. Verification

- [x] 2.1 Validate with targeted tests, repository-wide build/test, and OpenSpec checks
