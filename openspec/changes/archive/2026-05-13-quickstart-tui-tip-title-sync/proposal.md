## Why

The quickstart guide body already describes bare `lango` as the mission workbench, but the tip heading still says `Interactive TUI Chat`. That title no longer matches the actual surface.

## What Changes

- Rename the quickstart tip heading to a neutral `Interactive TUI` label that matches the workbench/cockpit split.
- Extend downstream docs requirements so the quickstart tip title does not regress back to chat-only wording.

## Impact

- The quickstart guide's heading and body now tell the same story.
- New users no longer see a stale "chat" label above the workbench entrypoint.
