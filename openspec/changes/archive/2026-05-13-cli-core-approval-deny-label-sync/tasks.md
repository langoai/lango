## 1. Docs Sync

- [x] 1.1 Update `docs/cli/core.md` so inline approval interrupts use the unified `d/Esc` deny wording.

## 2. Spec Sync

- [x] 2.1 Record the first-touch approval-deny wording requirement in the OpenSpec delta.

## 3. Verification

- [x] 3.1 Run `openspec validate cli-core-approval-deny-label-sync --strict`.
- [x] 3.2 Validate and archive the OpenSpec change.
