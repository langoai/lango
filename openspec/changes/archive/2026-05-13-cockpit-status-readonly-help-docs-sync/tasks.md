## 1. Docs Sync

- [x] 1.1 Update cockpit feature docs to describe the Status page read-only interaction model.
- [x] 1.2 Update the downstream-docs-sync spec to require the same Status interaction coverage.

## 2. Verification

- [x] 2.1 Run `openspec validate cockpit-status-readonly-help-docs-sync --strict`.
- [x] 2.2 Archive the OpenSpec change.
- [x] 2.3 Run `openspec validate --specs`.
