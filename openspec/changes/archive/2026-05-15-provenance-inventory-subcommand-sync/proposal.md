## Why

The quick references already document the concrete provenance subcommands, but the README internal tree and architecture inventory still compress the family to `status/checkpoint/session/attribution/bundle`.

## What Changes

- update the README internal tree and architecture inventory to the current provenance subcommand slices
- sync the existing remaining-inventory guard and main specs with that more truthful provenance wording

## Impact

- more truthful provenance inventory docs
- better discoverability of checkpoint, session, attribution, and bundle commands
- stronger regression protection against broad family-only shorthand
