## Overview

This is a lightweight README truth-sync update. The detailed command reference already carries the stream-routing contract, so README only needs concise operator-facing reinforcement.

## Decision

- Add one summary paragraph under CLI Commands
- Add one summary sentence in the workbench/cockpit section
- Keep the wording high-level instead of duplicating the full command reference
