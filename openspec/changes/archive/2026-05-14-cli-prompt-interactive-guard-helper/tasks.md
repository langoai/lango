## 1. Shared Interactive Guard

- [x] 1.1 Add a reusable interactive-terminal guard helper to the prompt package
- [x] 1.2 Reuse the helper from security CLI commands that currently call `prompt.IsInteractive()` directly

## 2. Verification

- [x] 2.1 Add prompt helper regression coverage for the interactive-terminal guard
- [x] 2.2 Validate with package tests, repository-wide build/test, and OpenSpec checks
