# Proposal

## Why

The knowledge-exchange escrow path already lands `create + fund` and `release`, but it still lacks the first refund execution slice that connects funded review-path escrow to canonical refund execution evidence.

## What Changes

- add the first public architecture page for escrow refund
- expose a receipts-backed `refund_escrow_settlement` meta tool
- wire the page into the architecture landing page, track page, and docs navigation
- sync the OpenSpec requirements for docs and meta tools
- archive the completed change after sync
