## 1. Wrapper Param Guards

- [x] 1.1 Enforce the required `query` parameter for `search_learnings`.
- [x] 1.2 Add `save_learning` wrapper coverage for missing required inputs.
- [x] 1.3 Add `create_skill` wrapper coverage for missing required inputs.

## 2. Verification

- [x] 2.1 Run `go test ./internal/app -count=1`.
- [x] 2.2 Run `go build ./...`.
- [x] 2.3 Run `go test ./...`.

## 3. Spec Sync

- [x] 3.1 Update `meta-tools` and `production-readiness` coverage for learning/skill wrapper params.
- [ ] 3.2 Validate and archive the OpenSpec change.
