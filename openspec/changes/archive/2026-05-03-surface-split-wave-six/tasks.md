# Tasks

- [x] Create the `surface-split-wave-six` OpenSpec change artifacts
- [x] Add the new `mission-workbench-tui` spec as the bare-`lango` contract
- [x] Add delta specs for `mission-control-tui`, `tui-cockpit-layout`, `cockpit-shell`, `cockpit-pages`, and `interactive-tui-chat`
- [x] Close the current bare-`lango` main-spec drift in the OpenSpec deltas
- [x] Validate the change with `openspec validate surface-split-wave-six --strict`
- [ ] Add the standalone `internal/cli/workbench` root shell
- [ ] Route bare `lango` to the workbench while keeping `lango chat` and `lango cockpit` explicit
- [ ] Make Mission Control surface copy workbench-vs-cockpit aware
- [ ] Update public docs and CLI help text to match the landed routing
- [ ] Run implementation verification and complete the remaining OpenSpec workflow for the wave
