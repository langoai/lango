## Why

The chat transcript now shows tool lifecycle rows with compact param previews that persist through running, success, and error states. Detailed feature docs already describe that richer surface, but the README and CLI overview still stop at generic chat/approval summaries.

## What Changes

- Update the README cockpit/workbench overview to mention tool lifecycle transcript rows with compact param previews.
- Update the CLI core overview to mention the same running/completed tool-row visibility.
- Record the sync in OpenSpec.

## Impact

- Brings first-touch docs up to the current operator-facing chat transcript contract.
- Reduces drift between detailed feature docs and entry-point docs.
