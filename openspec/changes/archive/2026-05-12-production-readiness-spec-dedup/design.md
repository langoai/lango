## Overview

This is a spec-only consolidation change.

## Design Decisions

### Keep behavior, remove duplicate wording

The resulting requirement still covers:

- address timeout cleanup
- address companion-error cleanup

but only through one requirement block, so future readers do not have to reconcile overlapping text.
