# Tasks

- [ ] Update `agent-control-plane-tools` spec so `RequestedAgent` is advisory metadata and stored instructions remain raw
- [ ] Update `tool-capability-layer` spec so custom teammate paths use the current allowlist as their effective ceiling
- [ ] Run `openspec status --change teammate-routing-decoupling`
- [ ] Run `openspec validate teammate-routing-decoupling --strict`
- [ ] Run `go test ./...`
- [ ] Run `go build ./...`
- [ ] Archive the change after validation
