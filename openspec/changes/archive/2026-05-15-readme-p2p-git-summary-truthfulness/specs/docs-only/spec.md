## ADDED Requirements
### Requirement: README P2P git quick reference reflects current CLI behavior
The README quick reference SHALL describe the current `lango p2p git push` and `lango p2p git fetch` behavior truthfully.

#### Scenario: README git summaries stay guidance-oriented
- **WHEN** the current CLI still delegates live exchange to the running server and runtime/tool flows
- **THEN** the README SHALL not describe those commands as direct bundle execution
