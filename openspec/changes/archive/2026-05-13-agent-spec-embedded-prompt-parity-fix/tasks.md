## 1. Runtime Parity

- [x] 1.1 Make built-in runtime agent specs load embedded per-agent identity content consistently.
- [x] 1.2 Update parity regressions for the current built-in escalation wording and formatting normalization.

## 2. Verification

- [x] 2.1 Run `go test ./internal/orchestration -count=1`.
- [x] 2.2 Run `go build ./...`.
- [x] 2.3 Run `go test ./...`.
- [x] 2.4 Validate and archive the OpenSpec change.
