## 1. Doc Sync

- [x] 1.1 Update the P2P CLI git-bundle example to use actual tool names and parameter names.

## 2. Verification

- [x] 2.1 Run `openspec archive p2p-cli-git-example-truth-sync -y`.
- [x] 2.2 Run `openspec validate --specs`.

## 3. Spec Sync

- [x] 3.1 Update `downstream-docs-sync` coverage for the corrected git workspace example.
