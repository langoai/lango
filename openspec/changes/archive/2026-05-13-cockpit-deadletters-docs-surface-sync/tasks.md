## 1. Docs Sync

- [x] 1.1 Add a dedicated Dead Letters section to `docs/features/cockpit.md`.
- [x] 1.2 Describe filter keys, retry flow, and degraded/load-failure states.

## 2. Spec Sync

- [x] 2.1 Update downstream docs-sync requirements to keep the Dead Letters feature section aligned.

## 3. Verification

- [x] 3.1 Run `openspec validate cockpit-deadletters-docs-surface-sync --strict`.
- [x] 3.2 Archive the OpenSpec change.
- [x] 3.3 Run `openspec validate --specs`.
