## Overview

This is a contract-clarification change for the running follow-up replacement path.

## Design Decisions

### Make the next-turn semantics explicit

The replacement hotkeys do not merely change visible draft text; they change the actual prompt that `Enter` will run next. The docs and spec now say that directly.
