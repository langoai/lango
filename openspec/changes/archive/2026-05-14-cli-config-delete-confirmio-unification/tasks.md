## 1. Shared Confirmation Integration

- [x] 1.1 Replace the inline config delete confirmation parser with the shared prompt helper
- [x] 1.2 Preserve the existing `--force` bypass and user-visible prompt text

## 2. Verification

- [x] 2.1 Add config profile command regressions for approve, deny, and force flows
- [x] 2.2 Update the public config CLI documentation note
- [x] 2.3 Validate with package tests, repository-wide build/test, and OpenSpec checks
