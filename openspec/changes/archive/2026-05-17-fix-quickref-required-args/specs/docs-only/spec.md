## MODIFIED Requirements

### Requirement: Documentation-only updates
Documentation-only updates SHALL keep public command examples aligned with
implemented CLI command shapes.

#### Scenario: Quick reference commands are copy-paste safe
- **WHEN** public quick references list commands with required operands
- **THEN** those operands SHALL be shown instead of bare command roots
