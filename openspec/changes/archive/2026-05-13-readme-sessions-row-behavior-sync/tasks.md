## 1. Docs Sync

- [x] 1.1 Update the README cockpit shortcut table to describe Sessions newest-first behavior and empty-state split.
- [x] 1.2 Update the downstream-docs-sync spec to require the same README Sessions-row coverage.

## 2. Verification

- [x] 2.1 Run `openspec validate readme-sessions-row-behavior-sync --strict`.
- [x] 2.2 Archive the OpenSpec change.
- [x] 2.3 Run `openspec validate --specs`.
