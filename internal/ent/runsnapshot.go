// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/langoai/lango/internal/ent/runsnapshot"
)

// RunSnapshot is the model entity for the RunSnapshot schema.
type RunSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Run this snapshot belongs to
	RunID string `json:"run_id,omitempty"`
	// Session that owns this run
	SessionKey string `json:"session_key,omitempty"`
	// Status holds the value of the "status" field.
	Status runsnapshot.Status `json:"status,omitempty"`
	// Goal holds the value of the "goal" field.
	Goal string `json:"goal,omitempty"`
	// Full JSON-serialized RunSnapshot
	SnapshotData string `json:"snapshot_data,omitempty"`
	// Last journal event seq applied to this snapshot
	LastJournalSeq int64 `json:"last_journal_seq,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RunSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case runsnapshot.FieldLastJournalSeq:
			values[i] = new(sql.NullInt64)
		case runsnapshot.FieldRunID, runsnapshot.FieldSessionKey, runsnapshot.FieldStatus, runsnapshot.FieldGoal, runsnapshot.FieldSnapshotData:
			values[i] = new(sql.NullString)
		case runsnapshot.FieldCreatedAt, runsnapshot.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case runsnapshot.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RunSnapshot fields.
func (_m *RunSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case runsnapshot.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case runsnapshot.FieldRunID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field run_id", values[i])
			} else if value.Valid {
				_m.RunID = value.String
			}
		case runsnapshot.FieldSessionKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_key", values[i])
			} else if value.Valid {
				_m.SessionKey = value.String
			}
		case runsnapshot.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = runsnapshot.Status(value.String)
			}
		case runsnapshot.FieldGoal:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field goal", values[i])
			} else if value.Valid {
				_m.Goal = value.String
			}
		case runsnapshot.FieldSnapshotData:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot_data", values[i])
			} else if value.Valid {
				_m.SnapshotData = value.String
			}
		case runsnapshot.FieldLastJournalSeq:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_journal_seq", values[i])
			} else if value.Valid {
				_m.LastJournalSeq = value.Int64
			}
		case runsnapshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case runsnapshot.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RunSnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *RunSnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RunSnapshot.
// Note that you need to call RunSnapshot.Unwrap() before calling this method if this RunSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RunSnapshot) Update() *RunSnapshotUpdateOne {
	return NewRunSnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RunSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RunSnapshot) Unwrap() *RunSnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RunSnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RunSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("RunSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("run_id=")
	builder.WriteString(_m.RunID)
	builder.WriteString(", ")
	builder.WriteString("session_key=")
	builder.WriteString(_m.SessionKey)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("goal=")
	builder.WriteString(_m.Goal)
	builder.WriteString(", ")
	builder.WriteString("snapshot_data=")
	builder.WriteString(_m.SnapshotData)
	builder.WriteString(", ")
	builder.WriteString("last_journal_seq=")
	builder.WriteString(fmt.Sprintf("%v", _m.LastJournalSeq))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RunSnapshots is a parsable slice of RunSnapshot.
type RunSnapshots []*RunSnapshot
